var f = $(x)
{
    switch (x)
    {
        case 0:
            println("zero");
            break;
        case 1:
            println("one");
            break;
        default:
            println("other");
    }
};

for (var i = 0; i < 5; ++i)
{
    f(i);
}
