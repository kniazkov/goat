import "list.goat";

var list = new List();
for (var i = 0; i < 10; i++)
    list.push(i);

var li = list.insertAfter('a', list.find(5));
li = list.insertBefore('b', li);
li = list.insertAfter('c', li);
println(list.toArray());
