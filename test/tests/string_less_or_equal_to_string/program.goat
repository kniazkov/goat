var a = ["Abracadabra", "The", "Goat", "Programming", "Language"];
var t = "Language";
for (var i = 0; i < a.length(); i++) {
	print("\'" + a[i] + "\' " + (a[i] <= t ? "less than or equal to": "greater than") + " \'" + t + "\'\n");
}
