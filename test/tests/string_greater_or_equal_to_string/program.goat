var a = ["Abracadabra", "The", "Goat", "Programming", "Language"];
var t = "Language";
for (var i = 0; i < a.length(); i++) {
	print("\'" + a[i] + "\' " + (a[i] >= t ? "greater than or equal to": "less than") + " \'" + t + "\'\n");
}
