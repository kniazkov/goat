var parent =
{
    child :
    {
        init : $(x)
        {
            this.x = x;
        },

        get_x : $()
        {
            return x;
        }
    }
};

var obj = new parent.child(13);
println(obj.get_x());
