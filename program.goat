var obj =
{
    data : {},

    set : $(key, value)
    {
        println("setter called, value = " + value);
        if (key.instanceof(String))
        {
            data[key] = value;
            println("new value is " + value);
        }
    }
};

obj["value"] = 0;
obj[1] = 1;
//obj[false] = 2;

println(obj.data);
