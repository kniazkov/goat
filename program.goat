var c = '$';

var thr = thread(n)
{
    for (var i = 0; i < n; i++)
    {
        print(c);
    }
};

var dll = dlopen("ext/ext_lib.dll");
println(dll);
var fn = dll.find("do_something");
println(fn);
var test = [1, 2, "test", 3.1, 4, null, true, false, '$', {one: 1, two: 2}, thr];
println(test);
var val = fn(test, thr);
println(val);
