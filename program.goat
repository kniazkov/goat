import "timer.goat";

var x = 0;

println("start");
var i = Timer.setInterval($()
{
    x++;
    port["gpio0"] = !port["gpio0"];
    if (x == 100)
    {
        i.stop();
    }
}, new Timer.Milliseconds(100));
