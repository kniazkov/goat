import "timer.goat";

var count = 50;

var i = Timer.setInterval(
	$() {
		print("" + count + "\n");
		count = count - 1;
	},
	new Timer.Milliseconds(50)
);

while(count >= 0)
{
}

i.stop();
print("------------\n");
print("" + i.ticks + "\n");
print("" + i.skipped + "\n");
