import "timer.goat";

var count = 10;

var i = Timer.setInterval(
	$() {
		print("" + count + "\n");
		count = count - 1;
	},
	new Timer.Nanoseconds(100)
);

while(count >= 0)
{
}

i.stop();
print("------------\n");
print("" + i.ticks + "\n");
print("" + i.skipped + "\n");
